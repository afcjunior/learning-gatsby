import styled from "styled-components"

export const HeaderContainer = styled.header`
  h1 a {
    color: #222222;
    font-size: 2em;
    font-weight: bold;

    &:hover {
      text-decoration: none;
      border-bottom: none;
    }
  }
`
